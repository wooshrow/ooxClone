class Main
{
    static void main()
    {
        Number a := new Number(1);
        Number b := new Number(2);
        int r := x.Add(b);
        assert r == 3;
    }
}

class Number
{
    int value;

    Number(int value)
    {
        this.value := value;
    }

    int Add(Number that)
    {
        int x := this.value;
        int y := that.value;
        return x + y;
    }
}